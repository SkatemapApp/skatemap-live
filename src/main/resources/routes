# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Location update endpoint for skating events
PUT     /skatingEvents/:skatingEventId/skaters/:skaterId    controllers.LocationController.updateLocation(skatingEventId, skaterId)
